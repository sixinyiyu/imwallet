.DS_Store
.idea/
*.code-workspace
output/
node_modules/


# Environment files (contain secrets)
.env
.env.*
!.env.example

# RSA keys (generated at runtime, never commit)
*.pem

# Android build output
android/

# PID files (local dev runtime)
.pids/

# Project notes (internal, not for commit)
document.md

# QAgent screenshots
qagent_screenshot_*.png

# Rust workspace & server
apps/server/target/
target/
apps/server/.cache/
apps/server/output/
apps/server/.env

# Docs (not for commit)
docs/